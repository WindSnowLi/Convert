# 📦 部署脚本总览

本项目提供了完整的 Linux 部署解决方案，支持多种部署方式。

## 📂 文件结构

```
convert/
├── 🚀 部署脚本
│   ├── deploy_linux.sh        # Linux 常规部署（Systemd）
│   ├── deploy_docker.sh       # Docker 部署脚本
│   ├── deploy_menu.sh         # 交互式部署菜单 ⭐
│   └── test_deployment.sh     # 部署验证测试
│
├── 🐳 Docker 配置
│   ├── Dockerfile             # Docker 镜像定义
│   ├── docker-compose.yml     # Docker Compose 配置
│   └── .dockerignore          # Docker 构建忽略文件
│
├── 🛠️ 辅助工具
│   ├── Makefile               # 快速命令工具
│   └── test_ncm.py           # NCM 功能测试
│
└── 📖 文档
    ├── Linux部署指南.md       # 详细部署文档
    ├── 快速部署参考.md        # 快速参考卡
    └── 本文件
```

---

## 🎯 脚本功能说明

### 1️⃣ deploy_linux.sh - 常规部署

**功能：** 完整的 Linux 系统部署方案

**特点：**
- ✅ 自动检测操作系统（Ubuntu/Debian/CentOS/RHEL）
- ✅ 智能检测重复部署
- ✅ 自动安装所有依赖
- ✅ 配置 systemd 服务
- ✅ 开机自启动
- ✅ 日志管理

**使用场景：** 生产环境、长期运行

**命令：**
```bash
sudo bash deploy_linux.sh
```

**检测逻辑：**
- 应用目录 `/opt/media-converter`
- Systemd 服务 `media-converter.service`
- 端口占用检测
- 进程 PID 文件

**重复部署选项：**
1. 重新部署 - 删除所有旧资源
2. 更新部署 - 保留数据，更新应用
3. 退出

---

### 2️⃣ deploy_docker.sh - Docker 部署

**功能：** 使用 Docker 容器部署应用

**特点：**
- ✅ 环境隔离，不污染系统
- ✅ 自动检测 Docker 环境
- ✅ 智能检测已有部署
- ✅ 支持 Docker 和 Docker Compose
- ✅ 容器健康检查
- ✅ 资源限制配置

**使用场景：** 快速部署、测试环境、容器化运行

**命令：**
```bash
bash deploy_docker.sh
```

**检测逻辑：**
- 容器名称 `media-converter`
- 镜像 `media-converter:latest`
- 端口映射检测
- 容器状态

**重复部署选项：**
1. 重新部署 - 删除容器和镜像
2. 重启容器 - 保持现有部署
3. 更新容器 - 重新构建
4. 退出

---

### 3️⃣ deploy_menu.sh - 交互式菜单 ⭐

**功能：** 友好的交互式部署界面

**特点：**
- ✅ 图形化菜单界面
- ✅ 一键选择部署方式
- ✅ 实时状态查看
- ✅ 统一管理入口
- ✅ 新手友好

**使用场景：** 不熟悉命令行、需要可视化操作

**命令：**
```bash
sudo bash deploy_menu.sh
```

**菜单选项：**
1. 常规部署（Systemd）
2. Docker 部署
3. Docker Compose 部署
4. 查看部署状态
5. 卸载应用
6. 查看帮助文档

---

### 4️⃣ test_deployment.sh - 部署验证

**功能：** 全面测试部署是否成功

**特点：**
- ✅ 端口监听检测
- ✅ HTTP 服务测试
- ✅ API 端点验证
- ✅ 进程状态检查
- ✅ 依赖完整性检测
- ✅ 网络连通性测试
- ✅ 响应时间测试

**使用场景：** 部署后验证、故障排查

**命令：**
```bash
bash test_deployment.sh
```

**测试项目：**
- 端口 52113 监听状态
- HTTP 200 响应
- API /formats 端点
- Systemd/Docker 进程
- Python/FFmpeg 依赖
- 文件和目录完整性
- 网络连通性和响应时间

---

## 🛠️ Makefile - 快速命令

**功能：** 简化常用操作命令

**使用方法：**
```bash
make help          # 显示帮助
make install       # 安装依赖
make run           # 运行应用
make deploy-linux  # Linux 部署
make deploy-docker # Docker 部署
make deploy-menu   # 部署菜单
make status        # 查看状态
make logs          # 查看日志
make restart       # 重启服务
make clean         # 清理文件
make uninstall     # 卸载应用
```

---

## 🔄 重复部署检测机制

### 为什么需要检测重复部署？

1. **避免冲突** - 防止端口、服务名冲突
2. **数据保护** - 避免误删用户数据
3. **灵活选择** - 提供更新、重启等多种选项
4. **用户体验** - 智能提示，避免错误操作

### 检测流程

```
启动部署脚本
    ↓
检测系统环境
    ↓
扫描已有部署 ←─┐
    ↓           │
是否已部署？    │
    ├─ 否 → 全新安装
    └─ 是 ↓
显示部署信息
    ↓
用户选择操作
    ├─ 重新部署 → 清理旧部署 ──┘
    ├─ 更新部署 → 保留数据，更新
    ├─ 重启服务 → 仅重启
    └─ 退出
```

### 常规部署检测项

| 检测项 | 路径/名称 | 说明 |
|-------|----------|------|
| 应用目录 | `/opt/media-converter` | 主程序目录 |
| 系统服务 | `media-converter.service` | Systemd 服务 |
| 端口占用 | `:52113` | 应用监听端口 |
| 进程文件 | `/var/run/media-converter.pid` | PID 文件 |
| 日志目录 | `/var/log/media-converter` | 日志存储 |

### Docker 部署检测项

| 检测项 | 名称/标识 | 说明 |
|-------|----------|------|
| 容器 | `media-converter` | 容器名称 |
| 镜像 | `media-converter:latest` | 镜像标签 |
| 端口映射 | `52113:52113` | 端口绑定 |
| 容器状态 | running/stopped | 运行状态 |

---

## 📊 部署方式选择建议

### 场景推荐

| 场景 | 推荐方式 | 原因 |
|------|---------|------|
| 生产服务器 | deploy_linux.sh | 性能最优，深度集成 |
| 测试环境 | deploy_docker.sh | 快速部署，易于重置 |
| 开发调试 | 手动运行 | 灵活修改，即时生效 |
| 新手用户 | deploy_menu.sh | 界面友好，操作简单 |
| CI/CD | Docker Compose | 标准化，易于自动化 |

### 性能对比

| 指标 | 常规部署 | Docker 部署 |
|------|---------|-------------|
| 部署时间 | 5-10 分钟 | 3-5 分钟 |
| 内存占用 | ~200MB | ~350MB |
| CPU 占用 | 低 | 略高 |
| 启动时间 | 2-3 秒 | 5-8 秒 |
| 性能损耗 | 0% | <5% |

---

## 🔐 安全性说明

### 权限要求

- **常规部署** - 需要 root 权限（sudo）
- **Docker 部署** - Docker 组权限
- **运行应用** - 以 root 运行（生产环境建议创建专用用户）

### 安全建议

1. ✅ 使用防火墙限制访问
2. ✅ 配置 Nginx 反向代理
3. ✅ 启用 HTTPS
4. ✅ 限制文件上传大小
5. ✅ 定期更新依赖
6. ✅ 监控日志文件

---

## 🆘 故障排查

### 常见问题

**Q: 端口被占用怎么办？**
```bash
# 查找占用进程
sudo netstat -tlnp | grep 52113
sudo ss -tlnp | grep 52113

# 修改端口（编辑 app.py）
```

**Q: 权限不足？**
```bash
# 使用 sudo
sudo bash deploy_linux.sh

# 或添加 Docker 权限
sudo usermod -aG docker $USER
```

**Q: FFmpeg 安装失败？**
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install ffmpeg

# CentOS/RHEL
sudo yum install epel-release
sudo yum install ffmpeg
```

**Q: 服务启动失败？**
```bash
# 查看日志
journalctl -u media-converter -n 50
docker logs media-converter

# 测试端口
curl http://localhost:52113/formats
```

---

## 📚 相关文档

- 📖 [README.md](README.md) - 项目总览
- 🐧 [Linux部署指南.md](Linux部署指南.md) - 详细部署教程
- ⚡ [快速部署参考.md](快速部署参考.md) - 快速参考卡
- ☁️ [NCM格式说明.md](NCM格式说明.md) - NCM 格式文档

---

## 💡 使用建议

### 首次部署

推荐使用 **deploy_menu.sh**：
```bash
chmod +x deploy_menu.sh
sudo bash deploy_menu.sh
```

选择你喜欢的部署方式，脚本会自动处理所有细节。

### 生产环境

推荐配置：
1. 使用 deploy_linux.sh 部署
2. 配置 Nginx 反向代理
3. 启用 HTTPS（Let's Encrypt）
4. 设置日志轮转
5. 配置监控告警

### 快速测试

最快方式：
```bash
bash deploy_docker.sh
```

5 分钟内即可完成部署并访问应用。

---

## 🎯 最佳实践

1. **部署前** - 先运行 test_deployment.sh 检查环境
2. **部署后** - 再次运行 test_deployment.sh 验证
3. **使用 Makefile** - 简化日常管理命令
4. **定期备份** - 备份 uploads 和 outputs 目录
5. **查看日志** - 遇到问题先查看日志

---

**祝你部署顺利！** 🚀

如有问题，请查阅详细文档或提交 Issue。
