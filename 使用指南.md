# 🎯 快速开始指南

## Windows 部署

### 第一步：安装 FFmpeg（音视频转换必需）

### 方法 1：自动安装（推荐）⭐
双击运行 **`安装FFmpeg.bat`** 文件，按照提示操作。

### 方法 2：手动安装
1. 下载 FFmpeg: https://ffmpeg.org/download.html
2. 解压到任意目录（如 `C:\ffmpeg`）
3. 将 `bin` 目录添加到系统环境变量 PATH
   - 右键"此电脑" → 属性 → 高级系统设置
   - 环境变量 → 系统变量 → Path → 编辑 → 新建
   - 输入 FFmpeg 的 bin 目录路径（如 `C:\ffmpeg\bin`）
4. 重新打开终端，验证安装：`ffmpeg -version`

## 第二步：启动应用

双击运行 **`启动.bat`** 文件

服务器启动后，会自动显示访问地址。

## 第三步：访问网页

在浏览器中打开：**http://localhost:52113**

---

## Linux 部署

### 🚀 快速部署（推荐）

#### 方式一：交互式菜单
```bash
# 1. 添加执行权限
chmod +x setup.sh deploy_menu.sh

# 2. 运行设置脚本
bash setup.sh

# 3. 启动部署菜单
sudo bash deploy_menu.sh
```

#### 方式二：一键脚本

**常规部署（Systemd）：**
```bash
chmod +x deploy_linux.sh
sudo bash deploy_linux.sh
```

**Docker 部署：**
```bash
chmod +x deploy_docker.sh
bash deploy_docker.sh
```

#### 方式三：使用 Makefile
```bash
make deploy-menu    # 显示菜单
make deploy-linux   # 常规部署
make deploy-docker  # Docker 部署
```

### 📝 部署说明

**常规部署特点：**
- ✅ 使用 systemd 管理服务
- ✅ 开机自启动
- ✅ 性能最优
- ✅ 适合生产环境

**Docker 部署特点：**
- ✅ 环境隔离
- ✅ 快速部署（5 分钟）
- ✅ 易于迁移
- ✅ 适合测试环境

**智能检测：**
- 两个脚本都会自动检测是否已部署
- 提供重新部署、更新部署等选项
- 防止重复部署导致的冲突

### 🔍 验证部署

```bash
# 运行测试脚本
bash test_deployment.sh

# 或使用 make
make test
```

### 管理命令

**Systemd 服务：**
```bash
systemctl status media-converter   # 查看状态
systemctl start media-converter    # 启动
systemctl stop media-converter     # 停止
systemctl restart media-converter  # 重启
journalctl -u media-converter -f   # 查看日志
```

**Docker 容器：**
```bash
docker ps -f name=media-converter  # 查看状态
docker start media-converter       # 启动
docker stop media-converter        # 停止
docker restart media-converter     # 重启
docker logs -f media-converter     # 查看日志
```

**使用 Makefile：**
```bash
make status    # 查看状态
make logs      # 查看日志
make restart   # 重启服务
```

详细文档：查看 [Linux部署指南.md](Linux部署指南.md)

---

## macOS 部署

```bash
# 1. 安装 Homebrew（如果未安装）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. 安装 FFmpeg
brew install ffmpeg

# 3. 安装 Python 依赖
pip3 install -r requirements.txt

# 4. 运行应用
python3 app.py
```

---

## 第三步：访问网页

在浏览器中打开：**http://localhost:52113**

### 手机访问（可选）
1. 确保手机和电脑在同一个 WiFi 网络
2. 查看电脑的 IP 地址（如 192.168.1.100）
3. 在手机浏览器访问：**http://192.168.1.100:52113**

## 📝 使用说明

1. 点击上传区域或拖拽文件到页面
2. 选择需要转换的格式（音频/视频/图片）
3. 点击"开始转换"按钮
4. 等待转换完成，文件会自动下载

### ☁️ 网易云音乐 NCM 格式

- 支持直接上传 `.ncm` 文件
- 自动解密并显示歌曲信息（歌名、歌手、专辑）
- 可转换为任意音频格式（MP3、FLAC 等）
- 无需安装网易云音乐客户端

## ⚠️ 注意事项

- 首次使用需要安装 FFmpeg
- 最大支持 500MB 的文件
- 转换过程可能需要一些时间，请耐心等待
- 如果转换失败，请检查 FFmpeg 是否正确安装

## 🆘 常见问题

**Q: 点击"启动.bat"后出现错误？**
A: 请确保已经安装了 Python 依赖包，项目会自动创建虚拟环境。

**Q: 转换音视频时提示 "FFmpeg 错误"？**
A: 请确保 FFmpeg 已正确安装并添加到系统 PATH。

**Q: 网页打不开？**
A: 检查防火墙设置，或尝试访问 http://127.0.0.1:52113

**Q: 转换速度很慢？**
A: 这取决于文件大小和电脑性能，请耐心等待。

## 💖 享受使用

这个工具界面美观，操作简单，专为你打造！

祝使用愉快！💝
