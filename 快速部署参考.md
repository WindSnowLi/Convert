# 🚀 快速部署参考卡

## ⚡ 一键命令

### Windows
```powershell
# 双击运行
启动.bat

# 或 PowerShell
.\start.ps1
```

### Linux

#### 交互式菜单（推荐）
```bash
chmod +x deploy_menu.sh
sudo bash deploy_menu.sh
```

#### 快速部署
```bash
# 常规部署
sudo bash deploy_linux.sh

# Docker 部署
bash deploy_docker.sh

# Docker Compose
docker compose up -d --build
```

---

## 📋 部署方式选择

| 如果你... | 推荐方式 | 命令 |
|----------|---------|------|
| Windows 个人使用 | 直接运行 | `启动.bat` |
| Linux 生产环境 | 常规部署 | `sudo bash deploy_linux.sh` |
| 快速测试 | Docker | `bash deploy_docker.sh` |
| 开发调试 | 手动运行 | `python app.py` |
| 多实例部署 | Docker Compose | `docker compose up -d` |

---

## 🔍 部署检测逻辑

### 常规部署脚本检测
✓ 应用目录 `/opt/media-converter`  
✓ Systemd 服务 `media-converter.service`  
✓ 端口占用 `52113`  
✓ 进程 PID 文件  

### Docker 部署脚本检测
✓ 容器名称 `media-converter`  
✓ 镜像 `media-converter:latest`  
✓ 端口映射 `52113:52113`  

### 重复部署处理
1. **重新部署** - 删除所有旧资源
2. **更新部署** - 保留数据，仅更新应用
3. **重启服务** - 不做更改，仅重启
4. **退出** - 取消操作

---

## 🛠️ 常用管理命令

### Systemd 服务（常规部署）
```bash
# 状态
systemctl status media-converter

# 启动/停止/重启
systemctl start media-converter
systemctl stop media-converter
systemctl restart media-converter

# 日志
journalctl -u media-converter -f
tail -f /var/log/media-converter/app.log
```

### Docker 容器
```bash
# 状态
docker ps -f name=media-converter

# 启动/停止/重启
docker start media-converter
docker stop media-converter
docker restart media-converter

# 日志
docker logs -f media-converter

# 进入容器
docker exec -it media-converter bash
```

### Docker Compose
```bash
# 启动
docker compose up -d

# 停止
docker compose down

# 重启
docker compose restart

# 日志
docker compose logs -f

# 重新构建
docker compose up -d --build
```

---

## 🗑️ 卸载命令

### Windows
```
直接删除文件夹
```

### Linux 常规部署
```bash
sudo bash deploy_linux.sh uninstall
```

### Docker 部署
```bash
bash deploy_docker.sh uninstall
```

### Docker Compose
```bash
docker compose down
docker rmi media-converter:latest
```

---

## 🌐 访问地址

### 本地访问
```
http://localhost:52113
```

### 局域网访问
```
http://你的IP:52113

# 查看 IP
Windows: ipconfig
Linux: ip addr show 或 hostname -I
```

### 生产环境（配置 Nginx）
```
http://your-domain.com
https://your-domain.com
```

---

## ⚠️ 常见问题快速解决

### 端口被占用
```bash
# 查找占用进程
sudo netstat -tlnp | grep 52113
sudo ss -tlnp | grep 52113

# 杀掉进程
sudo kill -9 <PID>

# 或修改端口（编辑 app.py）
```

### 权限不足
```bash
# Linux 使用 sudo
sudo bash deploy_linux.sh

# Docker 添加用户到 docker 组
sudo usermod -aG docker $USER
# 重新登录
```

### FFmpeg 未安装
```bash
# Ubuntu/Debian
sudo apt-get install ffmpeg

# CentOS/RHEL
sudo yum install epel-release
sudo yum install ffmpeg

# macOS
brew install ffmpeg

# Windows
# 运行: 安装FFmpeg.ps1
```

### 服务启动失败
```bash
# 查看详细日志
# Systemd
journalctl -u media-converter -n 50

# Docker
docker logs media-converter --tail 50

# 检查端口占用
sudo netstat -tlnp | grep 52113
```

---

## 🔐 安全配置

### 防火墙
```bash
# UFW (Ubuntu)
sudo ufw allow 52113/tcp

# Firewalld (CentOS)
sudo firewall-cmd --permanent --add-port=52113/tcp
sudo firewall-cmd --reload
```

### 生产环境建议
- ✅ 使用 Nginx 反向代理
- ✅ 配置 HTTPS (Let's Encrypt)
- ✅ 限制上传文件大小
- ✅ 配置日志轮转
- ✅ 定期备份数据

---

## 📊 性能优化

### Systemd 服务
```ini
# 编辑 /etc/systemd/system/media-converter.service
[Service]
LimitNOFILE=65535
```

### Docker
```yaml
# docker-compose.yml
deploy:
  resources:
    limits:
      cpus: '2.0'
      memory: 2G
```

### Nginx 配置
```nginx
client_max_body_size 500M;
proxy_read_timeout 300;
proxy_connect_timeout 300;
```

---

## 💡 最佳实践

1. **开发环境** → 直接运行 `python app.py`
2. **测试环境** → Docker 部署
3. **生产环境** → 常规部署 + Nginx + HTTPS
4. **个人使用** → Windows `启动.bat`

---

## 📞 帮助资源

- 📖 [README.md](README.md) - 项目总览
- 🐧 [Linux部署指南.md](Linux部署指南.md) - 详细部署文档
- ☁️ [NCM格式说明.md](NCM格式说明.md) - NCM 格式说明
- 📝 [使用指南.md](使用指南.md) - 快速开始

---

**Happy Deploying! 🎉**
